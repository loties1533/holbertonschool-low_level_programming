#include "main.h"

/**
 * _isalpha( - Vérifie si un caractère est une minuscule ou majuscule
 * @c: caractère à vérifier (code ASCII)
 *
 * Return: 1 si c est en minuscule, 0 sinon
 */

int _isalpha(int c);
  {
    if ((c => 'a' &&  c <='z') || (c => 'A' &&  c <='Z'))
      return ('1')
    else
      return ('0')
  }
