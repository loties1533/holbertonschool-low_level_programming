## Concepts clés appris
- Utilisation avancée de `malloc` et `free`
- Gestion des erreurs d’allocation mémoire
- Calcul de tailles avec `sizeof`
- Réutilisation de fonctions standards (comme `calloc` et `realloc`)
- Bonnes pratiques d’allocation et de libération mémoire

## Compilation
Tous les fichiers sont compilés avec :
```bash
gcc -Wall -pedantic -Werror -Wextra -std=gnu89